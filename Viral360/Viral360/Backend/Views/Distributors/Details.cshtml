@model Uprise.Models.Distributor

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Distributor</h4>
    <hr />
@if (Request.IsAuthenticated && ViewBag.Id == Model.UserId)
{
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DistributorStatus.StatusName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DistributorStatus.StatusName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UserId.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserId.Email)
        </dd>

    </dl>
}
else{
    <p> Unothorized to view this page.</p> 
    }
    
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
